This is the main template: Title in external is $title
@{[ do {
	#Can even commen
	BEGIN {
		use Time::HiRes qw<time>;
		use JSON;
	}
	encode_json  [map time."\n", (1..10)];
}
]}

@{['other perl code']}

@{[inject("header.plex")]}

	

